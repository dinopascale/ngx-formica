import { FrontSideDirective } from './front-side.directive';

describe('FrontSideDirective', () => {
  it('should create an instance', () => {
    const directive = new FrontSideDirective();
    expect(directive).toBeTruthy();
  });
});
