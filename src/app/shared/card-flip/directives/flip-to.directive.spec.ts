import { FlipToDirective } from './flip-to.directive';

describe('FlipToDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipToDirective();
    expect(directive).toBeTruthy();
  });
});
