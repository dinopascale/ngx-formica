import { BackSideDirective } from './back-side.directive';

describe('BackSideDirective', () => {
  it('should create an instance', () => {
    const directive = new BackSideDirective();
    expect(directive).toBeTruthy();
  });
});
